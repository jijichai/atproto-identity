import * as plc from '@did-plc/lib';
import { PlcDatabase } from './types';
declare type Contents = Record<string, plc.IndexedOperation[]>;
export declare class MockDatabase implements PlcDatabase {
    contents: Contents;
    close(): Promise<void>;
    healthCheck(): Promise<void>;
    validateAndAddOp(did: string, proposed: plc.OpOrTombstone): Promise<void>;
    opsForDid(did: string): Promise<plc.CompatibleOpOrTombstone[]>;
    indexedOpsForDid(did: string, includeNull?: boolean): Promise<plc.IndexedOperation[]>;
    lastOpForDid(did: string): Promise<plc.CompatibleOpOrTombstone | null>;
    exportOps(_count: number, _after?: Date): Promise<plc.ExportedOp[]>;
}
export default MockDatabase;
